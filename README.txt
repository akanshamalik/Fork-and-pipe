Read from the file and get the sum of the number:

Three files are given: small.txt, medium.txt and large.txt

Using child and parent processes and connecting using fork() and pipe()-
-Open a data file.
-Read each line of String - each line has exact 5 characters(3 digit plus return and new line)
-Convert the string to an integer.
-Calculate the sum of all integers in a data file.

When running its output also ask the user the number of process theywant for child an parent process.
